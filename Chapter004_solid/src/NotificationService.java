public class NotificationService {
    public void sendNotification(String message) {
        // Code to send notification (e.g., email)
        System.out.println("Notification sent with message: " + message);
    }
}
